syntax="proto3";
package snail.stream;

message CommonReq {
    uint64 reqid = 1;
    map<string, string> headers = 2;
}

message CommonResp {
    uint64 reqid = 1;
    int32 code = 2;
    string msg = 3;
    map<string, string> headers = 4;
}
